//Efrain Retana Segura UNA
/*
#include <cstdlib>
#include <iostream>

using namespace std;

int t = 0;

int f(int);
int g(int&);

int main(int argc, char** argv) {
    int x = 4;
    int y;

    y = f(5) + f(6);
    cout << "y = " << y << endl;
    y = f(5) + f(6);
    cout << "y = " << y << endl;
    cout << endl;

    if (f(2) == f(2)) {
        cout << "OK.." << endl;
    }else {
        cout << "Ooops.." << endl;
    }
    cout << endl;

    int z = 8;
    cout << "f(" << z << ") = " << f(z) << endl;
    cout << "f(" << z << ") = " << f(z) << endl;

    return 0;
}

int f(int p) {
    t++;
    return 2 * p + t;
}

int g(int& p) {
    p++;
    return p + 3;
}
*/