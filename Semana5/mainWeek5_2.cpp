//Efrain Retana Segura UNA
#include "clases2 .h"

#include <iostream>
#include <cstdlib>

/*
using namespace std;

int main(int argc, char** argv) {

    a2 w;

    b2* x1 = new b2();
    w.p1(*x1);

    b2* x2 = new b2(); // Use b2* for polymorphism
    w.p1(*x2);

    x1->m1();
    x2->m1(10); // Assumes m1(int) is virtual and implemented in b4

    cout << endl;

    b4* y1 = new b4(*x1); // b4(b2&) constructor
    w.p1(*y1);
    y1->m3();

    b4* y2 = new b4(*x2); // b4(b2&) constructor, x2 is b2* pointing to b4
    w.p1(*y2);
    y2->m3();

    cout << endl;

    delete x1;
    delete x2;
    delete y1;
    delete y2;

    return 0;
}
*/